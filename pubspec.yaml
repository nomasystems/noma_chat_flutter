name: noma_chat
description: Chat SDK, persistent cache and UI Kit for Noma platform
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  web_socket_channel: ^3.0.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.8.0
  hive_ce: ^2.7.0
  cached_network_image: ^3.4.1
  emoji_picker_flutter: ^4.3.0
  just_audio: ^0.9.42
  path_provider: ^2.1.5
  record: ^6.0.0
  url_launcher: ^6.3.1
  html: ^0.15.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  fake_async: ^1.3.0
  build_runner: ^2.4.0
  freezed: ^3.1.0
  json_serializable: ^6.6.1
  golden_toolkit: ^0.15.0
